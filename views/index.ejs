<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
    <title>Lista de Tarefas</title>
</head>

<body>
<% if (mensagem != "") { %>
    <section class="mensagem mensagem-<%= tipoMensagem %>">
        <p><%= mensagem %></p>
        <span id="fechar-mensagem"><i class="bi bi-x"></i></span>

    </section>
<% } %>

    <header>
<% if (tarefa == null) { %>
    <%- include('./components/form', {tituloForm: "LISTA DE TAREFAS", action: "create", value: "", idBtn: "btnAdicionar", nameBtn: "Adicionar"} )  %>
<% } else { %>
    <%- include('./components/form', {tituloForm: "EDITAR TAREFA", action: `updateOne/${tarefa._id}`, value: tarefa.tarefa, idBtn: "btnAtualizar", nameBtn: "Atualizar"})%>
<% } %>
    </header>

    <section id="tarefas">
        <ul>
<% listaTarefas.forEach((tarefa)=> {%>
            <li class="check-<%= tarefa.check %>">
                <div>
                    <a href="/check/<%= tarefa._id %>" class="check-<%= tarefa.check %>"><%= tarefa.tarefa %>  </a>
                    <span>Criado em <%= tarefa.date.toLocaleDateString('pt-BR') %></span>
                </div>
                <div>
                    <a class="btnUpdate" href="/getById/<%= tarefa._id %>/update"><i class="bi bi-pencil-square"></i></a>
                    <a class="btnDelete" href="/getById/<%= tarefa._id %>/delete"><i class="bi bi-trash"></i></a>
                </div>
            </li>
<% }) %>
        </ul>
    </section>

<% if (tarefaDelete != null) { %>
    <%- include('./components/modalDel', {tarefa: tarefaDelete}) %>
<% } %>

    <script src="script.js"></script>
</body>

</html>